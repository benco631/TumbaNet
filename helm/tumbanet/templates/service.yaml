apiVersion: v1
kind: Service
metadata:
  name: {{ include "tumbanet.fullname" . }}
  labels:
    {{- include "tumbanet.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tumbanet.selectorLabels" . | nindent 4 }}
