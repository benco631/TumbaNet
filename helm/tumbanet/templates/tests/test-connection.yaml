apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tumbanet.fullname" . }}-test-connection"
  labels:
    {{- include "tumbanet.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tumbanet.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
